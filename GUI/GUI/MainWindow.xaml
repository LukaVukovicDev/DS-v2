<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Co-Working Management" Height="600" Width="900">
    <DockPanel>
        <!-- Naslov sa imenom lanca -->
        <TextBlock Text="{Binding ChainName}" 
                   FontSize="20" FontWeight="Bold" 
                   Margin="10" DockPanel.Dock="Top"/>

        <TabControl>
            <!-- Korisnici -->
            <TabItem Header="Users">
                <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                        <DataGridTextColumn Header="Membership" Binding="{Binding MembershipType}" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Lokacije -->
            <TabItem Header="Locations">
                <DataGrid ItemsSource="{Binding Locations}" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="City" Binding="{Binding City}" />
                        <DataGridTextColumn Header="Max Capacity" Binding="{Binding MaxCapacity}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Rezervacije -->
            <TabItem Header="Reservations">
                <DataGrid ItemsSource="{Binding Reservations}" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="User" Binding="{Binding UserName}" />
                        <DataGridTextColumn Header="Resource" Binding="{Binding ResourceName}" />
                        <DataGridTextColumn Header="Start" Binding="{Binding StartTime}" />
                        <DataGridTextColumn Header="End" Binding="{Binding EndTime}" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>