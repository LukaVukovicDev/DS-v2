<Window x:Class="GUI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Login" Height="220" Width="350"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Name="UsernameBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Password:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button Content="Login"
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Height="35"
                Margin="5"
                Click="Login_Click"/>

    </Grid>
</Window>